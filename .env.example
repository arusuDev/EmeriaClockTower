# Discord bot token (required)
DISCORD_TOKEN=your-bot-token-here

# Optional: set to a guild ID during development for instant slash-command sync.
# If unset, commands are registered globally (may take up to ~1 hour to appear).
# DISCORD_GUILD_ID=123456789012345678
